# UART-
Implemented UART communication protocol coded in pure verilog which has 16x sampling with different module architecture along with separate always blocks for sequential and combinational  circuits within same module to make code more readable and simple 
